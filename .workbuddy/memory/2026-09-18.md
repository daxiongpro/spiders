
## spiders 仓库精简（2026-09-18）
- 目标：把 spiders 整理成可复用、精简的代码仓库。
- 删除：约 80 个 _*.txt/_*.log 诊断日志；废弃一次性脚本 auto_run/batch_transcript/categorize_batch4/classify_batch1-3/cross_tab/filter_money/gen_first_md/gen_lists/gen_migration_plan/local_transcribe/merge_and_verify/pick_and_download/probe_audio/run_all_categories/run_hotspot/split_batches/test_concurrency/verify_data/verify_detail_download；冗余数据 batch_*.json/douyin_favorites_*/douyin_raw_zhengqian/videotab_raw/money_list/migration_*/probe_result/transcript_*/three_old_videos/douyin_cookies/_auth；多余文档(6份交接/说明 md)；目录 minutes/tests/__pycache__；src 下非本链路爬虫 douyin_spider/redbook_spider/quanminkge_spider；output/douyin 整个浏览器用户目录(几百MB着色器/LevelDB缓存)。
- 保留：核心流水线 fetch_only/lark_transcribe/status/run_pipeline/progress_dashboard/cleanup + src/douyin_api+douyin_sign + 9个list清单 + README.md/.gitignore/requirements.txt + output/douyin/storage_state.json(登录态,gitignored)。
- 代码改进：fetch_only/lark_transcribe/cleanup 的 BASE 默认由硬编码D盘改为相对脚本自身目录(os.path.dirname(__file__))，换机器无需改代码；cleanup.py 默认OUT也改相对。
- 清理输出目录中间产物(_trash 5.44GB/_tr 64MB/已转写_temp_audio 66个mp4 288MB)共 5.78GB，由 cleanup.py --do 完成，MD成品(762篇)不受影响。
- 验证：status.py 正常运行，762/762 满贯，路径解析正确。仓库不含.git/.workbuddy 约 0.54MB。

## spiders 目录二次整理：按类型归位到子目录（2026-09-18 续）
- 背景：根目录仍散落 9 个 list_*.json 与 6 个 py 脚本，用户要求"整理成文件夹格式"。
- 新结构：根目录下建 `scripts/`（6 个入口脚本）、`config/`（9 个 list_*.json 分类清单）；`src/`、`output/`、`requirements.txt`、README.md、.gitignore 留在根目录。
- 路径适配（关键）：所有脚本原 `BASE = dirname(__file__)`（脚本在根）改为 `BASE = dirname(dirname(__file__))`（项目根=spiders），并新增 `LIST_DIR = BASE/config`；run_pipeline/progress_dashboard 原硬编码 `C:/Users/Administrator/Desktop/spiders` 也改为动态 `dirname(dirname(__file__))`，FETCH/TRANS 指向 `scripts/`，清单 glob 改为 `config/`。这样移动文件后无需改代码即可定位 config/src/output。
- 修正：status.py 自检行 `n_list` 漏改仍指向 BASE 根目录（显示 0），已改为 LIST_DIR。
- 验证：py_compile 8 个脚本全过；status.py 实跑「清单 9 个 OK / 762/762 满贯」；run_pipeline 导入后 SEQINFO=762 且 FETCH/TRANS 路径存在；progress_dashboard.compute_status 返回 762/762/100%。运行解释器用 `C:/ProgramData/miniforge3/python.exe`（已装 av 18.1.0 + requests）；managed python 3.13 缺 av 不可用。
- 注意：run_pipeline.py 内 `PY = C:/ProgramData/miniforge3/python.exe` 仍硬编码解释器路径，换机若路径不同需改或加检测。
