# 【闪客】一小时从 Transformer 到大模型！ #ai新星计划#大模型#transformer#科普#AI

- 链接：https://www.douyin.com/video/7679400007291686195
- 原文件夹：未分类
- 新分类：学习·成长
- 点赞数：1.3万
- 相关主题：

## 口播逐字稿

Speaker 1 00:00:00.080 
一年前我出了一期视频叫一小时从函数到Transformer，又出了一个半途而废的系列叫白话Transformer。评论区的催更呢我都看到了，一直没敢回。看过的可以扣个1，两个都看过的扣个2，两个都看过的一直等更新没等到，想杀了我的可以扣个圆周率的前 10 位。今天我终于可以把这块补齐了，一方面是我觉得当时我并没有能力把后半部分讲清楚，另外一方面我现在回过头再看之前的一小时从函数到 Transformer 这个视频，前面讲的还可以，后面的 Transformer 讲的是真的烂。时隔一年之后呢，我已经完成了进化。今天我不但会把 Transformer 的原理从头到尾给你讲清楚，还会带着你看看你知道的大模型最前沿是怎么在这个基础之上做改进的。本期视频不需要你有任何的前置知识，当然如果你看过我之前的视频是最好的，没看过也没有关系，你只需要跟着我的视频一步一步往前走，我保证你会对当今大模型的底层原理和近代发展有一个全网最通透的了解。

Speaker 1 00:00:56.030 
好了，我们开始吧！现代大语言模型的起点非常非常简单，我们就是要找到这样一个函数，它的输入是多个词，输出是预测的下一个词。比如像这样的飞天闪客，或者今天天气真好。怎么样找到这个函数呢？其实之前的一小时的视频解决的就是这个问题。如果你没看过也没有关系，浓缩成一句话的话，就是这个世界上的任何函数都能用一个神经网络表示。而神经网络如果浓缩成一句话的话，它就是个通用的函数形式，里面包含了未知参数。下面呢，这个就是最简单的形式了。如果把这个激活函数也去掉的话，就是简简单单的一个线性函数而已。

Speaker 1 00:01:37.930 
公式里的 X 对应下面这个X， Y 就对应下面这个Y。这里的输入 X 可以变成多个。比如 X1 对应X1， X2 对应X2，同理呢，输出 Y 也可以变成多个。我们就可以根据大量已知的 X 和 Y 的值，逐步求解出未知的 W 和 B 了。左边呢，就是我们常说的训练数据，右面就是我们的模型参数或权重。只不过在大模型的视角里，这两部分的量都特别特别大而已，其他的没什么本质区别。比如即便是不大的 GPT 2 中， FFN 层的输入就有 768 个神经元，中间的隐藏层达到了 3072 个，而最后线性投影层的输出更是高达 50257 个。

Speaker 1 00:02:18.150 
现在看不懂这个图，不要害怕，之后的每一层我都会讲到。虽然似乎好像已经把这两层的样子讲完了，你看我就说很简单吧。好了，回到刚刚这个图，之后呢，为了画图的方便，我把它竖过来，如果你实在不习惯的话，就把手机也竖过来看吧，开玩笑。我们知道神经网络的输入可以是多个数字，输出也可以是多个数字。但是你看现在的输入和输出都是文字，自然第一步要做的事情已经写在你脸上了，就是把文字变成数字，而且很自然的会想到应该把每个词都分别变成一组数字。至于为什么要变成一组数字，而不是一个数字呢？具体又怎么变成数字的，我们先不考虑。你现在可是个架构师，不屑于讨论这些细节，反正就给我变就行了。

Speaker 1 00:03:00.750 
最后呢，再看看输出，那自然也是要把神经网络输出的一组数字，再变回成文字。当然这块具体是怎么变的，我们倒可以现在就设计一下。比如说我们让输出的这组数字中的每个位置都表示一个词，然后看输出这些数字哪个位置比较大，比如说这里就是 98 比较大，那么对应的就是好这个词。这样就成功的把这组数字转换成文字了。

Speaker 1 00:03:24.230 
当然这块你也看到了，用这个方法的话，这组数字的长度必须囊括所有词的个数，也就是整个词表的大小。刚刚也提到了， GPT 2 最终模型的输出的个数是50257，那这个正是这里的词表大小。当然，其实只要改变一下输出的定义，你可以用同样一套架构完成多种不同的任务。比如说呢，判断一下这句话的场景分类，或者说判断这句话的褒贬，仅仅就需要两个数字就搞定了。总之呢，归根结底，所有问题都可以转化成一个分类问题，只不过预测下一个词这个问题的类型特别特别多而已。好了，输入和输出的结构搞定了，输出的数字变成文字的流程也设计好了，那接下来我们就研究一下如何把输入的文字变成数字这一步。再简单点说，如何把单独的一个词变成数字？当然这件事我们得好好说道说道，我们就下个视频见吧。那每期视频的最后呢，我就随性跟大家聊几句天吧。其实我觉得 AI 领域很多事情，初学者之所以不理解，或者说讲解的人没有讲解清楚，我觉得大多数情况啊，其实最核心的问题就是没有把问题的定义搞清楚。其实很多 AI 领域的问题，只要把输入是什么、输出是什么的定义讲清楚了，那中间无非就是用一个神经网络强行求解，根本就没有什么复杂性。

Speaker 1 00:04:41.200 
整个这个系列呢，最后你会发现，其实 Transformer 内部就是大大小小的各种神经网络，每个大大小小的神经网络，其实最关键的问题还是它的定义，也就是输入和输出是什么，向量的长度有多大。因为除了这些之外，剩下的东西也没什么好研究的，它就是个神经网络嘛，只要靠大量的输入和输出训练数据去训练就可以了，没什么道理可讲。我们所需要做的无非就是把问题拆成一个个神经网络的输入和输出，让它们方便训练而已。

Speaker 1 00:05:08.280 
所以说我觉得对于初学者，如果你感到非常困惑的话，这一节的定义问题其实非常非常关键，同时你也会发现，我在后面的讲解的各个环节的时候，都是把如何定义输入和输出作为最关键的一个环节。而且你不知不觉会发现，只要把定义这个事搞清楚了，之后具体实现理解起来就非常非常简单了。所以我也希望你在看接下来的视频的时候，永远把定义问题作为首要理解问题的关键。

Speaker 1 00:05:33.520 
好了，不废话吧，我们一起加油吧！假如有这么多男生和女生，每个人用一个数字表示，现在你要根据这个数字编号进行性别的分类，你会怎么做呢？通过观察法可以看出女生的编号都是 2 的幂次，因此我们可以把这个特征用于划分男女。但如果我们换一种标号方式，每个人用两个数字编号表示，这时候你会怎么分类呢？没错，这时候只要看第一个数字是 0 还是 1 就可以了，是不是比刚刚的划分算法更简单了呢？下面我们回归到这个事的数学本质，这是一个一维的数轴，你可以把黑色的点想象成刚刚的男生，粉色就是女生。

Speaker 1 00:06:18.340 
现在我让你用一根线把它们一分为二，你该怎么做呢？做不到是不是？或许只能用一根非常拐弯的线才能做到。假如我们把它升级到二维空间，可以想象成刚刚用两个数字来表示一个人的情况，可以非常简单的用一条直线来划分区域了。这里的第一个数字就可以用来表示性别这个特征，甚至我们还可以用第二个数字表示性格是内向还是外向，也能用一条直线来进行划分。

Speaker 1 00:06:48.690 
好了，刚刚一个数字的情况，我们可以叫它一维向量，后来升级成两个数字的情况，我们可以叫它二维向量。显然二维向量在信息表达上更加灵活，也更容易变得线性可分。冷知识，把一个向量从低维变成高维，就叫做升维，反之叫做降维。好了，例子举完了，我们回到主战场文字。现在我们知道了，只用一个数字表示一个文字，显然灵活度是不够的，需要用一个维度足够多的向量来表示。不过在此之前，我们先把最小的单位从一个字改成一个词，这个最终让大模型理解的最小单位就叫做Token。把一个句子分割成 Token 这个过程就叫做分词Tokenize。

Speaker 1 00:07:32.570 
好了，接下来我们看看每个 Token 对应的向量应该长什么样呢？我们可以给每个位置人为定义一个特征，比如这个位置表示实物的属性。苹果的实物属性很高，喜欢的实物属性几乎没有，我的实物属性可能稍微有点吧。把所有的位置的值都填好，你就成功用一个向量表示了一个单词。那我们能用这样的表示做什么呢？这些高维向量处在高维空间，所以不好画图。那如果把它类比到二维空间，就可以很直观的观察到它们的位置关系，相当于这仨词的距离都非常远，一点都不搭嘎。而苹果这个词最终可能和香蕉很近，因为都是水果。同时呢，又可能和小米很近，因为都是电子设备。

Speaker 1 00:08:18.610 
当然，更准确说是向量的方向一致，如果用数学语言表达的话，就是两个向量的点积或余弦相似度较大。回到刚刚，我们怎么做才能如此完美的让它具有刚刚说的性质呢？如果像刚刚那样人工定义每个维度的特征含义，这几乎是不可能完成的任务，因为每个词的含义和变化太丰富了，就算真的有个神人把这样的向量搞出来，每个特征的含义可能是极其抽象的，这显然是不现实的。

Speaker 1 00:08:48.590 
我们该怎么办呢？当然有一种极端的办法，就是向量的维度就是整个词表的大小，每个词只有一个位置是1，这种编码方式叫做 one hot。中文翻。译过来就叫做独热编码，顾名思义，只有一个位置是热乎的。但是这样有两个问题，一个是词表太大会导致向量的维度太大，另一个呢，是无法度量词语之间的相似性，因为无论谁和谁的点击或余弦相似度都是0，很不友好。

Speaker 1 00:09:16.600 
那这可怎么办呢？答案很简单，就是不要去人为定义每个特征的含义到底是啥，爱啥啥，每个位置就先随机初始化一个值，作为模型中的参数，指定一个类型的任务去训练。就好了，训练出几就是几，至于是什么含义不管。正如那句古话所说，遇事不决上神经网络嘛。要怎么训练呢？首先把所有词的词向量拼接成一个大矩阵，纵向就是词表大小，横向就是每个词的向量维度大小。一开始这些值都是随机的，我们准备好大量的文本句子作为语料，先分词，选择一个中间词，其他的作为上下文词。训练的目标就是让中间词的词向量与上下文词的词向量的点积越大越好。同时呢，选出一些不相关的负样本，让它们的点击越小越好。最终在大量语料正负样本训练过后，这个大矩阵就被训练好了，这就是 Word2Vec 的方法。不过细节上，它的中心词和上下文词是分别用了两个矩阵，这里讲解的时候就给它简化了。由这种方法训练出的词向量会表现出很多令人惊艳的效果，把它们降维到三维空间，方便观察，那可以观察到相似的词的距离很近，还会出现 Man 减 Woman 约等于 King 减 Queen 这样的神奇的语义计算结果。

Speaker 1 00:10:40.390 
不过话说回来， Word2Vec 有两个非常主要的问题。第一，每个词的词向量都是静态的、固定的，没有考虑上下文词的影响。比如这两句话中的苹果的意思就完全不同。第二，训练出的矩阵往往作为下游任务的一个固定的输入，并非端到端的训练结果，也就是下游任务在做训练时，往往要固化 Word2Vec 训练好的矩阵，有种割裂感。

Speaker 1 00:11:06.950 
为了解决这两个问题，以及随着算力的不断突破，在后来基于 Transformer 的 BERT 和 GPT 模型则是直接简单粗暴的随机初始化词向量矩阵。现在我们给它个正式的名字，就叫做嵌入矩阵 embedding matrix，作为进入 Transformer 模块的第一步。所以整个流程就是一句话先分词成一个一个的token，在计算机里呢，就是一个一个的数字。不过呢，它现在可以代表嵌入矩阵中的索引了，我们给它取名为ID。然后根据 ID 在嵌入矩阵中取出对应的词向量，那我们现在也正式叫它为词嵌入 token embedding，最后呢，送入 Transformer 模型，接着后面的流程。

Speaker 1 00:11:51.740 
随着整个 Transformer 最终的预测下一词的任务训练，最初的嵌入矩阵就随着训练的进行不断的更新好了。所以经历了这么多年的演进，最终的嵌入模型变成了最最简单的模样，仅仅是个随机矩阵而已。那对应的代码实现也非常简单，定义两个变量，一个叫 vocabulary size，表示词表大小，一个叫 embedding dimension，表示词嵌入的维度大小，直接再用 PyTorch 的 nn. embedding 就可以把这个矩阵创建出来了。

Speaker 1 00:12:23.060 
好了，现在我们就把如何将文字变成数字这个事讲清楚了。回到这张图里，这个把文字变成数字的模块，就可以正式起名为 Token embedding 词嵌入。同时我们也知道了，将一句话拆分成多个词的过程，叫 Tokenize 分词，分出的每个词叫 Token 词元。而这里的每个词向量的维度，此时我们也可以具象化一下了。比如说 GPT- 2 中就是768，快给自己鼓鼓掌吧！接下来我们再把这个神经网络给具象化一下，虽然这里画的很大，但这其实是最简单的部分，也是我觉得很多初学者对 Transformer 最困惑的地方。那我们仍然下个视频见喽！

Speaker 1 00:13:01.000 
其实呢，正如第一个视频所说的，定义问题在这里仍然非常关键。我们现在要解决的其实就是把一个文字变成数字，至于变成一个数字还是多个数字，从这个定义而言是无所谓的。只不过我们发现，如果只定义一个数字的话，那么后面神经网络的训练或者各种处理都十分的不方便。所以其实 AI 领域要解决的很多问题都是先把问题定义清楚，然后呢我们再考虑以什么样的形式喂给这个输入和输出，能更方便的为这个神经网络的训练，仅此而已。那我们人在这里做出的努力，就是决定了一个文字要转换成多少个数字比较合适。那么肯定知道单个数字是不好的，但是多个数字多少才是合适的呢？那这就需要不断的实践得出结论了。同时你再看输出，输出我们也有很多形式，能够让我们得到最终想要的预测下一个词的效果嘛。只不过我们发现把这个当成一个超级大的分类问题是比较友好且方便的，不但能够很直观的拿到下一个词是什么，而且我们也方便我们设置各种策略。比如说 top 5 的词中随便选一个，还是说只选择最大的那一个，还是什么其他的策略，都可以在这个模型架构不变的情况下，仅仅加薄薄的一层筛选就能做到了。我们所做的努力的地方，也就是整个问题的输入和输出，而最难的部分不能用什么数学公式表达的部分，还是交给神经网络，你看，万变不离其宗嘛。

Speaker 1 00:14:22.420 
上个视频我们把第一步的词嵌入搞定了，下一步就是要解剖这个神经网络。不过在此之前，我们还得改造一下这个输出结构。其实在真正的 Transformer 架构中，这个神经网络的输出是多个向量，当然这里的每个向量都是词表大小。我们这个模型的任务是预测下一个词，而预测下一个词仅仅只看最后一个向量就可以了。同时由于可以并行处理，你可以理解为每一个词都经过一个同样的神经网络后，都会有一个输出是它的下一个词。以最后一个神经网络为例，在 GPT 2 中，它的结构就是输入是768，输出是50257。那这个神经网络我们应该怎么样设计呢？也就是要设计多少层？每层要有多少个神经元呢？这个问题没有什么标准答案，也就是 AI 最玄学的部分了。

Speaker 1 00:15:08.230 
Transformer 是这样设计的，先来一层是 3072 个神经元，这通常叫做升维或者上采样。然后再来一层还原为 768 个神经元，叫做降维或者下采样。根据经验呢，这样呢能更好的在高维的潜空间中学到有用的特征。最后再来一层50257，即词表的大小。虽然从维度上来说，这也是升维或者上采样，但是我们通常把这一步叫做投影。而且正如之前所说，这个投影可以非常灵活的根据分类的数量，决定神经元的个数。而在 Transformer 中，前面的这个部分被叫做 FFN 前馈神经网络，其他地方也有叫 FC 或者 MLP 的，都没问题。后面的这部分叫做 Linear 线性投影，也有叫 projection 的，翻译过来直接就是投影，也没问题。

Speaker 1 00:15:52.210 
回到这张图，神经网络的部分我们也终于可以具象化一下了。它实际上是分成两个部分，第一部分就是从 768 到 768 升维再降维的 FFN 前馈神经网络，输出的向量长度完全不变。第二部分就是从 768 到 50257 的 Linear 线性投影。通常来说，这个 FFN 层可以堆叠好多层，你看，这就是输入和输出维度一样的好处。

Speaker 1 00:16:16.490 
图中呢，我们也可以简化一下，把这部分标记一下，知道它是很多层就好了。OK，现在这个模型，你只需要给到足够多的输入和输出文本，就已经可以训练了，是不是非常非常简单？当然了，这个模型还有一个致命的问题，也正是 Transformer 要解决的核心问题，你猜是什么呢？我们下个视频再说喽。

Speaker 1 00:16:37.210 
这个视频的最后我想说，很多人一提Transformer，第一个想到的就是什么 Attention 呀，注意力机制呀，什么多头注意力机制啊等等。但其实如果作为初学者，我觉得你最困惑的地方根本就不是什么注意力是怎么计算的。从整个模型所要解决的问题来说，或者说什么从第一性原理来说，这个大的神经网络才是最核心的，解决这个问题的最关键的一个环节。那为什么很多人不提这一点呢？因为这没什么可说的，这是一个非常显然的事情。你什么模型最终都是一个神经网络在起作用啊，所谓的什么注意力机制，说白了无非就是给这个神经网络的输入做一点手脚，让输入的信息中包含其他上下文信息，仅此而已。它更像是一个外挂，所以说对于初学者就有一个很割裂的点，对于懂行的人来说呢，神经网络就是一个默认的，根本就不用提的一个点，它就当你已经知道这一点了。

Speaker 1 00:17:26.760 
所以当大家热衷于讨论什么是 Transformer 啊，什么是Attention，什么是 QKV 向量等等等等，这些信息就导致你误以为这些东西才是一个架构的核心。但是从完全的小白来看的话，其实不是的。拦住你理解这个问题的关键就是在于它其实是由一个非常简单的神经网络来实现的，以及它的输入和输出是什么，怎么样把文字变成数字，怎么样把数字变成文字的，这才是影响你理解这个问题的关键。

Speaker 1 00:17:54.420 
如果你现在从这样一个视角来看整个架构的话，你会发现很多问题其实就相当简单了，尤其是我们之后讲 Transformer 注意力机制的时候，你会发现它所解决的问题非常非常简单，所使用的方法也非常简单。不信的话我们就继续看吧。上个视频说现在这个模型有一个致命的问题是什么呢？还记不记得之前说过，其实这个神经网络是通用的，可以用在每个词身上。比如说单独看最后这个神经网络。它的输入仅仅是真这一个词的信息，就预测出了下一个词好。但实际上呢，好这个词是它前面所有词共同作用的一个结果，所以前面词的所有词的信息都会影响到下一个词是什么。虽然我们知道神经网络特别厉害，但也有一个大前提，就是我们得给它足够的信息啊。至于具体怎么计算才是它的强项，就像你说我们现在的大模型，你再厉害也得依赖你给它足够的上下文。

Speaker 1 00:18:51.120 
那这该怎么办呢？其实很简单，我们只需要把它前面词的词向量都加进来就好了。别看注意力机制后面经历了这么多乱七八糟的演进。但是它所解决的根本问题就是这么个破玩意。具体来说就是我们得到了一个新的词向量，比如说叫真撇。接下来我们就在这个基础上一点点优化。首先我们现在是把所有词都无差别的加进来，但其实不同词应该重要程度也不一样。如果给每个词前面都加上不同的权重，也就是把单纯的求和升级为加权求和，好像是更合理一些。恭喜你，这就是个伟大的改进。接下来的问题就很明确了，这些权重值要怎么得到呢？可以这样想，这个系数可以理解为是想表达这个上下文词和当前词的相关性有多大。系数越大，表示最终要包含的信息就该多一些。而两个向量的相关性，我们可以用向量的点积来表示，即两个向量的每个位置处的值相乘再相加。

Speaker 1 00:19:48.380 
当然了，你也不用管它具体是什么含义，最重要的是这个公式包含了这两个词的全部信息，这对于神经网络来说就足够了，也是最关键的。但是也正因为如此，直接用词向量本身做点积，其实对于神经网络来说缺少了一定的灵活性和自由度。所以我们可以单独弄出一组新的向量，专门用于做点积的计算，而不影响原来词向量长什么样。甚至我们还可以再灵活一点，用于和其他词向量做点积的向量，和被其他词做点积的向量，也可以弄成不一样的。

Speaker 1 00:20:19.860 
那我们把用于和其他词做点积的向量叫做查询向量query，简称Q。用于被其他词做点击的向量叫做键向量key，简称K。比如我们要算针这个词和其他词的系数，那么就用针这个字的 Q 向量和其他所有词的 K 向量做点击。还没完，其实我们可以再再灵活一点，你看哈。

Speaker 1 00:20:42.000 
接下来我们是不是就要把这个系数作为词向量的权重进行加权求和了，对吧？那其实我们可以把加的向量值也和原始的词向量做个区分，那我们把它叫做值向量value，简称V。所以总结一下就是，我们先让每个词的向量都分裂出三个新向量，分别是Q、K、V，然后让 Q 向量和每一个 K 向量做点积，得到一个相关性系数，然后让这个系数分别和 V 向量相乘，最终加权求和得到一个新的向量。

Speaker 1 00:21:11.840 
那我们把这个由词向量进行加权求和，得到这个新的词向量的过程，就叫做Attention，注意力机制。恭喜你学会了 Transformer 中最核心的内容。再次回到这张亲切的图，我们就又可以在这个架构中增加一层了，就叫做 Attention 注意力层。而且这个结构的输入和输出的向量的长度是没有变化的，所以呢，我们也可以把它和 FFN 层放到一块，并且起一个新的名字，就叫 Transformer Block，它作为一个整体是可以堆叠很多很多层的。

Speaker 1 00:21:42.030 
当然了，刚刚这个过程肯定你有一个一直敢想，但没敢问出口的问题，就是这个 QKV 向量分别是咋来的呢？换句话说，怎么样得到这个 QKV 向量，能够更好的帮助后面的神经网络完成预测下一个词的任务呢？是不是不知道？没关系，在 AI 的世界里，不知道的事其实最简单了，就无脑上神经网络嘛。比如说拿第一个词为例，就用原始的词向量分别乘以三个可训练的矩阵就可以了。而乘以一个矩阵，本质上就是一个单层的神经网络嘛，而且是纯线性计算，没有激活函数，输入和输出的维度是完全一样的。这些个矩阵中的数字就相当于神经网络中的未知参数 W 和B，是可以训练出来的。一开始呢，我们赋个随机值就可以了。

Speaker 1 00:22:25.960 
所以你看，其实一切的本质就是，不知道的事就弄个神经网络去求解。那这里的 Linear 和 FFN 都是最简单的神经网络，一开始的 Token Embedding 也是从一个词嵌入矩阵中拿到向量，本质上也是个神经网络。 Attention 注意力层最重要的 QKV 向量的获取，同样也是一个简单的神经网络。不过呢，这个注意力层离现在最原始的 Transformer 的注意力层还差那么一丢丢距离，我们努力再把它补全。其实就是还是个灵活度的问题，你看，这里的每个词向量是分别分裂出了一组 QKV 向量，对吧？我拿其中一个词举例，把这个向量先画的长一点。后来人们就发现，可以让每个词分裂出多组Q、K、 V 向量，每组呢在自己的内部做注意力计算，最后再将加权求和的结果拼接起来，这样做的效果会比只分出一组要好。那我们把这里的每个分组叫做一个头head，而这种多个头分别进行注意力运算的新方式就叫做多头注意力， multi head attention，简称MHA。

Speaker 1 00:23:26.750 
好了，回到这张图，注意力层这里终于可以再次更新一下了。至此，原始的 Transformer 架构图你应该差不多都能看懂了，不过呢，还有一些小细节，我们下个视频一起全部梳理一遍，加油！

Speaker 1 00:23:41.640 
好了，你看定义问题这个事情是不是自始至终都贯穿在我们的讲解之中？首先从大模型的架构来看，整个模型的输入和输出是最关键的。同时呢，这里面起到最核心的作用，其实就是一个非常非常简单的多层神经网络。那今天讲的注意力，仅仅就是要处理一个特定的问题的外挂而已。而且呢，再回到注意力机制本身，它从一个宏观的视角看，也非常简单呀。它无非就是想把上下文信息加进来，以便让神经网络的输出中有这些信息。不然你神经网络再厉害，你不给它足够的信息也不行。它根据一个词的信息，永远不可能计算出由之前所有词的信息共同组成的计算逻辑，这个是显而易见的。既然它的作用是把上下文的信息都加进来，那其实我们根本就不用设计什么，最直观的加进来的方法就是加权求和嘛。所以你一定要知道，注意力机制的本质就是个加权求和，根本就没什么难度。以至于为什么一点点它变得越来越难呢？其实就是因为我们要不断优化里面的细节。

Speaker 1 00:24:39.420 
接下来的话就是这个系数是怎么来的？我们先想到的是可以通过词向量之间做点积来计算，又发现只通过词向量自己做计算的话，这个信息是不够灵活的，所以我们又把每个词向量都分别分裂成Q、K、 V 三个向量，再做计算。我们发现还不够灵活，于是又把Q、K、 V 分成了多个头，再拼接，最终才演化成了现在的多头注意力。

Speaker 1 00:25:01.980 
当然我们后面现代模型架构又继续演进了，你会发现这个地方的玩法相当之多，有的呢是为了解决K、 V 向量占用体积过大的问题，有的是为了解决上下文过长，做点积运算的时候计算量太大的问题。但是归根结底你要知道，他们的出发点无非就是想把这个上下文信息想办法加进来而已。

Speaker 1 00:25:21.620 
所以你也会看到了，所有组件最初的那个根都是特别特别特别简单的，而你只需要理解这个根，其他的一些细节上的优化手段也就没什么难度了。甚至你可以根据它是不是流行，是不是新的趋势，而选择性的学习，没必要全学。上一回我们很自信的展示了我们的架构和原始 Transformer 架构图的对比。当然这里我要严谨的说一下，原始的 Transformer 论文中的图是左面这样的，但是现在大语言模型基本上都只使用了 Transformer 解码器的部分。所以我简化成右边这个样子了。你只需要看右边就可以了。

Speaker 1 00:25:58.270 
回到这张图，我们找找对应关系。今天天气真，对应的就是这个 inputs 输入， token embedding 词嵌入，对应的就是这里的 input embedding，一个意思嘛，都是把文字转换成数字。 MHA 多头注意力，就是这里的 multi head attention，缩写了下而已。 FFN 前馈神经网络，就是这里的 feed forward，后面再加个network，也是同样缩写了一下。整个这一大坨是 transformer block 堆叠了 n 次，那对应的就是这里。

Speaker 1 00:26:25.870 
最后的 linear 线性投影，对应的就是这里的linear，变成一个词表大小的向量，每个位置都标注了各个词出现的可能性，对应呢就是这个 output probabilities，也就是我们只剩下三个地方没说了，即位置编码 positional encoding、残差连接和归一化 add and norm。以及最后的 Softmax 函数。哎，这些都是小case，我们一个个来看就好了。

Speaker 1 00:26:48.110 
先说这个 positional encoding 位置编码，回想一下我们加权求和的过程是这个样子的。前面我们说了，要想让这个神经网络训练出效果，需要给到足够多的信息。那现在这个真向量已经有了全部的上下文信息，你觉得还缺点啥呢？没错，就是缺少每个词的位置信息。

Speaker 1 00:27:07.830 
一个词的位置很远和一个词的位置很近，其实影响可能是不一样的。我们就可以再增加一个向量信息，表示这个词的位置。或者从更抽象的意义上讲，你不用管它表示啥，你只是增加了一个信息的维度，剩下的就交给神经网络。这个位置信息就可以像词向量一样，从一个可训练的嵌入矩阵中提取，那这个向量我们给它取一个特殊的名字，就叫位置编码。当然这个位置编码可以有很多种获取逻辑，这个我们后面再说。

Speaker 1 00:27:35.530 
然后我们再看这里， Add and Norm， Add 非常简单，用我们第一个视频中的图就可以说明了。你看，这就是个神经网络的公式，对吧？ Add 要做的，顾名思义，仅仅就是把输入这个向量的值原封不动的加到输出这里就可以了。画在图中呢，就是长这个样子。它还有个更经典的名字叫做残差连接 residual connection，这样可以让训练变得更稳定。画到我们架构图中就可以是这个样子。我们再来看Norm，全称是 normalization 归一化，它简单说就是把向量调整到一个更稳定的范围。具体来说呢，就让它们的均值更接近0，方差更接近1。比如原始的值可能对应的图像是长这个样子，归一化之后可能会变成这个样子。不严谨的说，它有点像把它们都压扁了，但是相对之间的差异又还保留着。这在我们的架构图中，哪一层需要对向量规整规整，就可以加一个归一化。比如说在。 MHA 和 FFN 层之后，都进行这样的处理，那就可以画成这样了。

Speaker 1 00:28:35.080 
好了， add 和 norm 在这里就都讲完了，最后就只剩下这个 Softmax 了。哎，这个更简单，回忆一下我们之前的一张图，当时呢，我们在定义这个输出的格式，是一个词表大小的向量，取出最大的值作为下一个词。其实我们现在得到这组数字呢，叫做 logits 分数，它是神经网络直接输出的结果，只要是最终最大的那个数字能够更好的预测下一个词，它的任务就完成了。但是呢，我们更希望它的可读性高一点，比如说让这些数字都表示概率，也就是让它们的值都在 0 到 1 之间，并且总和为0。那么这样一个转换，我们就可以用到 Softmax 函数来处理，和归一化有点异曲同工之妙，都是把它归拢在一个稳定的范围。所以再回到我们的图中， Linear 线性投影之后，我们就得到的不再是最终的结果了，而是 logits 分数。然后 logits 再经过一层 Softmax 处理，最终才得到输出的概率probabilities。

Speaker 1 00:29:32.380 
至此， Transformer 的所有组件全都搞定了，快给自己鼓鼓掌吧！好了，如果你是初学者的话，你可能觉得有点不可思议，哎？怎么不知不觉就把 Transformer 彻底搞懂了？其实 Transformer 原本就没有多复杂，我直接口述一下流程，再帮你回忆一下，就更简单了。首先就是我们输入了一句话，对这句话先进行分词，tokenize，分出的每个词叫做token，我们再从一个词嵌入矩阵中拿到词向量，就是 token embedding。这个嵌入矩阵呢，就是随机初始化的一个矩阵，随着模型一起训练出来的一个结果。现在不是我们拿到了每个词都变成了一个向量嘛，这个数字就能往神经网络里面输入了，进而进行计算。但是有个问题就是。它现在不包含上下文词的信息，所以我们还得想个办法把它们都包含进来，那就用到了加权求和，就引出了系数具体怎么算，这个就是 Transformer 核心注意力机制要解决的问题。具体来说呢，就是分成三个 QKV 向量， QKV 向量的获取，本质上也是一个非常非常简单的单层神经网络而已，就是一个可训练的矩阵嘛。

Speaker 1 00:30:36.750 
经过注意力机制后，我们就得到了一个包含上下文信息的词向量。当然还有一个小步骤，就是再增加一个位置编码，把位置的信息也加入进来。这个时候，最后一个词就是已经包含一整句话的信息了。后面的话，你可以用这个词向量输入到一个神经网络里面。根据后面的任务来灵活定义。比如说你的任务是想判断这句话是褒义还是贬义，经过你的神经网络后，最终的输出可能就是一个神经元，或者是两个神经元。如果你想判断这句话它属于哪个场景，是什么生活类的，还是金融类的，还是职场类的，可能有 100 个分类，最终的输出就定 100 个神经元。我们现在想要的是预测下一个词这个任务，我们比较好的定义方式就是弄一个特别特别大的词表，让它的输出值就是每个词出现的可能性，也就是概率。这个时候输出的神经元就非常非常多了。

Speaker 1 00:31:25.800 
比如说光 GPT- 2 这种小模型就有 5 万多个。中间这个大的神经元，我们说了，通常的玩法呢，是先上采样，再下采样一下，这个就是 FFN 前馈神经网络里面干的事。那个多头注意力和前馈神经网络，它们可以堆叠很多层，因为它们前后的输入和输出的向量的维度是不变的嘛，这个就是我们常说的 Transformer 的层。最后一层，也就是 Linear 线性投影，就是把我们的词向量直接投影成词表大小的向量，它其实也是个简单的单层神经网络，而且只有线性变换，没有激活函数。

Speaker 1 00:31:58.360 
最后一点细节的话，就是我们现在神经网络的输出可以理解成 logits 分数。他没有一个约束嘛。那我们希望它表示成概率的含义，这样我们读起来就更有实际的意义了。所以我们可以再经过一层 Softmax 函数，把它转化成概率，这就是神经网络的一个整体流程。

Speaker 1 00:32:15.070 
当然中间还有为了训练的稳定性而增加的小技巧，比如说残差链接 residual connection，还有归一化normalization，还有架构图中没有体现的 dropout 等等，那这些就不影响整体的理解了，当然对于模型的训练来说还是非常非常重要的。

Speaker 1 00:32:29.750 
你看我说了一遍之后，你是不是会发现它其实特别特别简单了？上个视频我们终于打通了 Transformer 的全部流程，现在我们把它简化一下，变成这样。也就是说，如果不看细节的话，我们得到了一个可以不断预测下一个词的模型。这样一个神奇的模型是怎么训练出来的呢？其实非常非常简单，你看 Transformer 的输入和输出是不是就是这个样子的？比如说这里的门字就表示前面的飞天闪客的读者这句话的下一个字。再比如说这里的黑字就表示前面的飞天闪客的读者们最帅了这句话的下一个字。

Speaker 1 00:33:08.310 
当然之前我们说了， Transformer 的输出只有最后一个字是我们关心的，但是在模型的训练阶段，其实每个位置处的值都可以被利用起来。所以我们只需要在网上找到海量的文本信息，就可以作为训练数据了。比如说我们找的就是这句话。我们就可以把这句话的一部分作为输入，看看模型的输出，也就是预测值是什么。我们再把训练数据的另一部分作为真实值，和预测值进行对比。接下来就是什么计算它们的误差，算损失函数、梯度下降、反向传播、更新参数了，直到我们满意为止。

Speaker 1 00:33:40.900 
这个过程相信看过 1 小时从函数到 Transformer 的朋友已经很熟悉了。而且这种文字接龙任务最舒服的地方就是。它根本不需要人工标注，只需要找到一段文本，就可以抠掉后面的词，形成一次训练，那简直是太方便了。这种不需要人工标注，让数据自己给自己当标准答案的方式叫做自监督学习， self supervised learning。而这种通过海量的文本数据训练，让模型具备初步的语言和知识能力的阶段，叫做预训练， pretrain learning。而我们的 ChatGPT 中的 GPT 全称就是 generative pretrain transformer，翻译过来就是生成式预训练Transformer。

Speaker 1 00:34:19.370 
哎，你可能觉得这也太无脑了，但即便是这样一股脑的喂了一大堆文本做了预训练的模型，只要参数和数据量给够，就可以涌现出很多神奇的能力。比如说我们输入这样一段文字，这个模型就会输出orange。从现在的视角看，你可能觉得它理所应当，但其实你仔细想一下，你并没有训练模型做翻译任务，但是呢，它却通过你给的例子，居然等价的做到了翻译功能。也就是说，你不用重新训练模型，只需要在输入里给它举几个例子，它就能照猫画虎的学会一个新任务。如果这件事成立的话，之前为了专门训练某一任务而训练的语言模型，就可以全部用一个超级通用的预训练大模型代替了。我们称这种从上下文中的示例中学习的能力叫做少样本学习， few shot learning。只要你能用语言把任务描述清楚了，那这个模型就是万能的，我们通过预训练搞出了一个通才。那很自然的，预训练之后还有一个阶段叫做后训练，用于把模型变成专才。

Speaker 1 00:35:21.350 
举个最简单的例子，比如说我们输入给模型这样一个问题，那如果只是在大量的文本中做文字接龙训练的纯预训练模型，它的回答可能也是跟你玩文字接龙。但其实我们想要的可能是下面这种，也就是希望它遇到问题的时候就别再玩接龙游戏了。而是直接回答你的问题。那这该怎么办呢？很简单，就继续用更多的文本进行训练，只不过这次训练的数据专门是问答形式的文本，不能再是随机的、海量的、粗糙的数据了。我们管这种用人工精心标注的数据做二次训练的方式叫做监督微调， supervised fine tuning，简称SFT。对比起来，预训练是自监督的方式，数据海量但粗糙，用来把模型先训练成一个通才。后训练是有监督的方式，数据量少但精致，用来把模型训练成专才。其实和人也是一样的，我们先在自然环境中接触大量的信息，自己消化。再专门找时间，比如说做题啊，或者做专项训练，以提升具体的能力。

Speaker 1 00:36:22.040 
当然了，随着现代模型训练范式的迭代，两者在量级上的差距正在逐渐缩小，甚至后训练变得越来越重要和通用。比如 GLM 5.3 的所有提升就均来自于后训练，所以它也常被大家称为后训练的神。当然这是后话了。当然这还没完，人们为了进一步榨干模型的智能极限，又想了一个一个的小妙招，并且这些小妙招呢，正逐渐变成现代大模型训练的重点。比如说这个问题，如果让模型直接给出最终答案，其实是很难的，因为大模型的底层原理就是不断的输出下一个词，它没有一个像人一样的心算过程，所有的话必须要说出来才行。所以你只需要在提示词中增加这样一句话，模型就会自说自话的把推理过程说出来了。由于模型把心里话说出来了，从效果上看就好像是智力水平增加了一样。这种让模型把推理过程写出来的方式叫做思维链， chain of thought，简称COT。哎，你看参数也没动，模型架构也没变，仅仅是增加了句提示词，就获得了智能上的提升，是不是非常划算？当然除了提示词中的这种表面技巧外，我们还可以直接在模型的训练阶段就激发它的推理能力。

Speaker 1 00:37:31.690 
最著名的方式就是强化学习了，具体还分为基于人类反馈的强化学习，简称RLHF，和基于可验证奖励的强化学习，简称RLVR。因为强化学习又是一个非常庞大的体系，这里就不展开了。好了，我们现在手里的模型就不仅会说，还会思考了，虽然本质上也是说。当然它从形式上看，仍然就只是个文字接龙游戏而已，充其量改造一下，也就能成为一个高级的智能客服吧。我们还能怎么样继续站。干模型的智能呢？我们下个视频见喽。上个视频说我们已经拥有了一个可以进行文字接龙的语言模型了，但最多呢也就是个智能客服，那怎么样才能继续更大限度的发挥它的价值呢？其实这个问题就是这几年全人类费尽心思在做的事情，那为了搞明白这里的来龙去脉，今天我们就做一个角色扮演的游戏。

Speaker 1 00:38:25.520 
整个混乱的起点就是这个古老的语言模型，小的语言模型一开始呢还是个智障，但随着模型的参数越来越大，居然在某个临界点涌现出了智能。那为了和之前这个智障模型做个区分，你在前面加了个大字。这就构成了现在常说的大语言模型，简称LLM。恭喜你发明了今天的第一个新词。大模型本身只能做文字接龙，就是不断输出下一个字。但如果只是这么用的话，看起来仍然像个智障。那如果把角色区分一下，人为划分成一问一答两个角色，就实现了第一个有点智能的使用方式，对话。

Speaker 1 00:38:58.690 
现在呢，我不管你是什么身份，立刻把自己想象成一个老板， LLM 就是你的员工，我们就叫他小 L 吧。只不过他服务你的方式有点特别，只能一问一答，然后就结束了，不能追问，这个非常重要，后面要考。接下来的任务就是，你要想尽办法压榨这个只会一问一答的小L，榨干他的全部剩余价值。你会怎么做呢？不过先别急，你先给自己每次和小 L 的对话起了个洋气的名字，叫prompt。恭喜你发明了今天的第二个词。你还发现这部分内容还可以进一步区分，有的部分是背景信息，有的部分是最终的指示。你把背景信息的部分单独起了个名，叫context，上下文。恭喜你发明了今天第三个词。同时有的时候你需要对小 L 进行追问，但是呢，刚刚说了他只能一问一答，不能追问。但是你想了个巧妙的办法，就是每次沟通前把你们之前的对话历史放到 context 部分，作为上下文信息。然后呢，再给出你的问题，伪装成多人对话。然后你又迫不及待给这些特殊的上下文信息起了个新词，叫memory，意思是大模型的记忆。这些 memory 还可以再次调用大模型进行总结，从而对它的记忆进行压缩，进而减少上下文的长度。恭喜你，一不小心就已经发明了四个新词了。

Speaker 1 00:40:10.370 
此时，一个原本只能进行词语接龙的小L，就成功被你玩出了可以对话，并且可以不断追问的优秀牛马员工了。当然，不久之后你就不满足于现状了。你发现的第一个问题就是小 L 没有上网查阅资料的能力，要么就不知道，要么就胡说八道，说的内容都是些过时的消息。不过这很简单呀，给小 L 准备一台电脑不就可以了？不可以，还是那个问题，小 L 本身只会词语接龙，其他任何逻辑都无法独立完成。那怎么办呢？好办，你就告诉小L，如果你需要上网搜索资料的话，就告诉你，然后你帮忙查完资料后再给他，不就行了？但很快你就发现这样好像显得自己有点蠢，到底谁才是牛马呀？于是呢，你把上网这部分逻辑写成了一段程序，让这个程序去代理你和小 L 进行沟通，并且完成搜索的任务。在外人看来，你仍然是一问一答就拿到了结果，只不过面向的是这个神秘的程序了。太妙了，这个发明可不得了。

Speaker 1 00:41:04.570 
这个神秘的程序似乎本身就拥有了智能，而且还是能操作工具的更高级别的智能，你给它取名叫智能体agent。你可能觉得写多段程序不是看起来很简单吗？怎么敢叫这么科幻的名字？哎，千万别有心理负担，一些早期所谓的智能体，其实现逻辑仅仅就是多加了一段 prompt 而已，那从现在的视角回看，当时简直就是一种诈骗。回到这里，既然这个 agent 能上网搜索内容了。那是不是也可以增加个搜索本地文档或数据库的能力呢？可以的，只不过搜索的方式和传统数据库不同，要使用向量数据库，把语义相近的片段找出来。你给这种通过语义匹配向量化的信息，并将其加入上下文以增强生成内容的可靠性的办法，叫做检索增强生成， Retrieval Augmented Generation，简称RAG。抱歉有股东北大碴子味。

Speaker 1 00:41:52.610 
刚刚这个联网搜索也起个名字叫 Web Search，算了， Drop the web， just search，这样呢 scope 显得更大一些，连 RAG 也算是 search 的一种了，都属于获取模型参数以外的信息的能力。好了，现在看看你造的孽吧，这么一会功夫已经发明了 8 个新词了。当然了，好戏还在后头呢。好了，现在的整体架构就是你和小 L 中间隔着一层 agent 程序，用于减少你和大模型直接沟通的次数，并且处理一些小 L 无法操作的东西，包括刚刚的搜索以及还可能出现的其他各种工具的调用。但这就有个问题，我们聚焦于 agent 和大模型的对话过程来看，如果这部分一直用自然语言来沟通，那这个 agent 代码可不好用程序来实现呢，鬼知道大模型会怎么描述自己的需求呢？所以最好有个约定，让大模型按照指定的、死板的格式来回复，比如说JSON，这样呢，程序就能直接很方便的解析了。

Speaker 1 00:42:45.620 
你给这种 agent 和大模型之间关于工具调用所约定的对话格式，叫做 function calling。其实呢，就是个约定罢了，就好像开发的时候，前端和后端约定这个接口格式一样。好了，我们再看看右边，这些工具的实现现在是写在 agent 的主程序里面的，没有跟核心功能解耦。如果是单独写成一个服务，那么 agent 的主程序如何发现并调用这个服务，就又需要一套约定的规范了。比如说约定好 tools list 的方法就是返回工具列表。 Toolcall 方法就是调用具体的工具等等，也就是一套约定而已。你给这边的约定也起了个名字，叫做MCP，翻译过来叫模型上下文协议。至此，架构就变成了这个样子。此时大模型就像个只会说话不会做的智者，而 MCP 服务就是能提供各种工具的程序集。

Speaker 1 00:43:32.390 
中间的 agent 就是个传话筒，把大模型的话转换成调用工具的代码，把工具调用的结果再原封不动的传话给大模型。同时呢，别忘了还要给你这个老板传话，主打一个我不生产信息，我只是信息的搬运工。现在我们聚焦于 agent 和你的对话之间，虽然最底层肯定还是文字，但是交互形式上可以非常丰富多彩，可以是像 CLI 一样的命令行窗口，也可以是一个编程 IDE 工具，还可以是一个更为通用的桌面助手，比如说最近爆火的Cloudbot、MultiBot、OpenCloud，当然这仨是一个东西。

Speaker 1 00:44:05.380 
这里说句题外话，我感觉 Cloudecode 这个名字起的实在太失败了，一开始呢，好多人认为它是个大模型的名字，但其实它是个编程agent。现在呢，好不容易大家接受它是个编程 agent 了，但其实它早就走上了通用 agent 的道路。我感觉要是名字起好了，就没现在的 Cloudbot 什么事了。当然 Cloudbot 的爆火和它的自身的很多设计有关。如果本期视频的点赞过亿，我将会专门出一期视频讲讲它。不过不论什么形式的智能体，都有一个统一的缺点。假设我们想完成这样一个任务，从一个英文 PDF 文档当中提取内容，翻译成中文，最后保存成 Markdown 格式。当然你可以直接把这个需求描述给Agent，让它自己策划整体的流程。但是如果这个流程相对稳定，每次重新让 Agent 自由发挥的话，不但不稳定，还非常浪费Token。比如说整个流程中提取 PDF 和保存 Markdown 这两步，完全可以固化成固定的脚本，中间的翻译直接和大模型沟通即可，整个流程就不需要任何一个中间的智能体插手了。

Speaker 1 00:45:03.010 
要固化这样一个流程，你可以通过编程的方式来实现。为了方便编写这种链式的任务，你又发明了一个新的编程框架，起名叫Langchain。为了照顾非程序员用户，你又发明了一种低代码的方式，就是在页面上傻瓜式的拖拽，上手难度更低，你给它起的名字叫Workflow，即工作流。

Speaker 1 00:45:21.530 
但是还有个问题，假如这个问题我们再变一下，比如说处理原始文档不只有PDF，还有可能是 Word 文档、 TXT 文档、 PPT 等，输出的格式也可能是HTML、PDF，甚至是一张图片。那么难道你要给这些所有的排列组合都写一套工作流吗？这显然是不合适的。当然了，你也可以写一堆 if else 做判断。但是如果你仍然希望用户是以自然语的方式触发这个任务，不牺牲这个体验，这个时候就又不好用程序来判断分值了，那该怎么办呢？你可以这样设计，准备一个目录，把所有可能涉及到的转换脚本全都写好放在这，然后呢，写个统一的说明文件，把整体的流程描述清楚，并且告诉 agent 根据文件的格式灵活选取指定的脚本。

Speaker 1 00:46:04.320 
然后呢，在给 agent 下达任务之前，加上这么一句话，先读取刚刚我们写好的那一大串要求，然后再按照要求完成任务。这样整个过程就既保证了一定的灵活性，同时呢，又变得比较可控。但是这不就又来了优化空间吗？我可以提前约定好某个指定的位置，在 agent 中写死一段程序，去读取这个位置的 skill 点md。还是相当于把这句话固化成了一段程序，这样呢就不用每次都加这么一句废话了。虽然你也知道这破玩意好像就是把提示词换了个地方存起来，但想了想还是给它起个新名字吧，就叫做Skill，即 Agent 的技能。好家伙，这是往游戏里的英雄方向设计了呀。

Speaker 1 00:46:44.590 
好了，现在这个 Agent 已经被你弄成这个鬼样子了，你又发现对于一个复杂的任务，可能 Agent 的上下文会变得非常大，于是呢，你又发明了个新概念叫Subagent，对于一些独立的子任务，可以单独在这个子 Agent 中完成。其实本质上就是做了嘛上下文隔离，子 agent 产生的上下文不会保留在主 agent 中，仅此而已。行了，再好好看看你造的这些孽吧。

Speaker 1 00:47:09.640 
好了，这期视频的起点就是这个大语言模型，我们看到了人们在它的基础之上，在应用层做了哪些改进，以榨干这个仅仅只会文字接龙的模型的所有智力。接下来我们再看一看这个模型本身的架构层面有哪些演进的路线呢？这块就不像人们发明 agent 的时候了，搞那么多虚头巴脑的名词。因为它必须是实打实的能提升模型性能的技术，所以看的会比较扎实一点。不过现在呢，我要告诉你一个很扎心的事情，就是现在的大模型架构跟原始的 Transformer 相比，已经有点像特修斯之船了。它每经过一段时间，这里面的一个组件就被替换成了另外一种实现形式，但是它的本质要解决的问题是不变的。那导致现在的大模型其实 Transformer 就几乎把所有零件全都换了个遍了，那你说它还是不是原始的 Transformer 呢？那从特修斯之船的例子其实就分成两派了，有的人认为呢，已经完全不是原始 Transformer 的样子了，应该改个名字。那有的人也认为它的本质不变，所以仍然是Transformer。

Speaker 1 00:48:08.690 
我们下个视频就来好好聊一聊现代大模型的架构演进，以及我们一起看一下这里面是否还有其他可能改进的空间呢？你会发现其实很少了。下个视频见喽。不知道隔了一期视频之后，你还记不记得我们之前梳理的 Transformer 架构长什么样子了？可以回看一下这张图，回忆一下。其实从整个架构层面看，我们就不用关注太多细节了。我把这张图简化成这个样子，其实就是前后一个编码器、解码器，用于把文字和数字进行转换，中间一个注意力层和前馈神经网络承担了大部分的计算任务。接下来我们就看看，在这个简单的架构之上，人们都做了哪些手脚和改进呢？为了搞明白这个问题，我梳理了从 2019 年的 GPT- 2，一直到 2026 年的 DeepSeek V4，共 67 个核心开源模型的架构变化。我发现这个 Transformer 架构的底座似乎已经没有多少新花样可以玩了，同时呢，模型与模型间的差距也在逐渐缩小。比如说你有没有感觉，似乎好像好久没有遇到一个让你感觉像当时 GPT- 4 那样震惊的模型了？先看编码器这部分。在原始的 Transformer 架构中，分成 Token embedding 和 Positional embedding 两部分。这里的位置编码就是在原向量的基础上，根据不同的词的位置，再加上一个新的向量。那具体加的这个向量是什么？ Transformer 原论文中是取个固定的值，具体说呢，是通过正余弦函数算出一组值，那这种方式称为固定位置编码。但是后来被一种叫做旋转位置编码的技术给淘汰掉了，这种方式呢，可以让向量间计算点积时得到一些友好的位置特性。

Speaker 1 00:49:44.540 
再后来呢，又有人发明了对旋转位置编码的扩展方法，YARN，目的是可以支持更长的上下文位置计算。甚至呢，还有人提出了一种叫NoPE，即没有位置编码的一种位置编码方式，而且呢，还证明了一些场景的有效性。看到了吧？就这么一个位置编码的创新，基本就被挖干净了呀。实际的应用呢，也基本是世界线收束了。好了，我宣布位置编码死。回到这里，再看个更夸张的。有的时候我们为了稳定训练，需要把向量控制在一个可控的范围内，同时呢，要保证它们之间相对的差距不变。比如说原来是这个样子，把均值控制为0，方差控制为1，就变成了这个样子。这种把数据变换到一个更稳定的范围的方法叫做归一化。在 Transformer 原始论文中，具体的归一化方法叫LayerNorm，即层归一化。而现在大模型更常用的一种叫法叫RMSNorm，更适合当代大模型的训练。还没完，这只是具体计算算法的不同，而这个归一化层呢，还可以安插在模型中的各种位置。比如说安插在注意力层之前叫做PreNorm，安插在注意力层之后叫做PostNorm，还可以前面也来一个，后面也来一个。还没完，接下来我们还会讲到残差连接，这个 posnorm 还可以像这样安插在残差连接之前，也可以像这样安插在残差连接之后。哎呀，我的天呐，这样组合起来可就老多了，但是不好意思，所有这些组合都被咱们伟大的人类玩过了。

Speaker 1 00:51:13.710 
还没完，这个归一化操作可不仅仅是作用于词向量，还可以作用在注意力层里面的 QKV 向量上。那用在 Q 向量上就叫做Qnorm，用在 K 向量上叫做Knorm，用在 V 上又叫Vnorm。还可以同时作用在 QK 上叫做QKnorm，同时作用在 KV 上叫做KVnorm。啊，不好意思，这些组合也全都被玩完了。你还能想到其他改动的点吗？归一化，死。来看看下个倒霉鬼是谁吧。

Speaker 1 00:51:41.850 
这俩大哥咱们先别动。刚刚我们不是提到了个残差链接吗？在原始的 Transformer 实现中，就是个简单的加法操作，这么多年来也没人敢动这里。但是后来咱们的字节、DeepSeek、 Kimi 分别提出了HC、MHC、 Attention Residual，让这个所有人都觉得是个默认的、肯定是正确的部分也被动了刀子。当然了，这块还比较新，目前还处于探索阶段，玩的花样呢也十分有限。残差链接，死。

Speaker 1 00:52:10.690 
接下来就剩这俩大哥了，让我们先给这个占地面积最大的FF，口瓢了， FFN 层动刀吧。这个 FFN 层其实就是一个普通的全连接神经网络，属于是深度学习入门级别的结构了，只不过在大模型这里面就占地面积特别大了，这里的每条线都可以表示一个参数，那如果它特别大的话，就很占地，怎么办呢？答案很简单，变小点就好喽。

Speaker 1 00:52:35.050 
那我们这样，先把这个大的 FFN 拆成两个小的FFN，最后呢，增加一个可训练的路由层。每个 token 的计算只路由到其中一个 FFN 上，这样呢，就能保证总参数量仍然是很多，但是推理时只激活少量的参数，做到了一种平衡。这里的每个小 FFN 的网络又叫做 expert 专家，整个结构呢，构成的就叫做混合专家模型MOE。后来 DeepSeek 又对这个网络进行了再次优化，比如说把专家拆分的更细，引入了共享专家等等等等，起了个新的名字，很粗暴，就叫做DeepSeekMOE。现在啊，你在很多模型名字上看到类似什么 A 多少多少 B 这种写法，就是 MOE 架构。表示总参数量是 397 B，每次呢只激活， 17B 的意思。不好意思又嘴瓢了。现在的大模型基本上都 MOE 化了，只是专家数和激活比例各有各的配法。为此呢，人们还发明了个新词，把之前所有参数都参与计算的模型叫做 Dense 稠密模型，现在只激活部分参数的叫 Sparse 稀疏模型，哎，其实就是MOE。死。

Speaker 1 00:53:44.400 
好了，就剩下这个最经典的注意力层了，是 Transformer 架构的一个精髓。而且呢，人们在这上花的功夫可以说是恐怖如斯，我就没见过这么卷的。有 Transformer 原始论文中的MHA，以及后来改良的。MQA、GQA，还有 DeepSeek 研发的MLA，还有稀疏类型的，比如说 DeepSeek 的DSA、SWA、CSA、 HCA 等等。以及把注意力改成线性的，比如说 Kimi 的KDA、GateDeltaNet、LightningAttention，还有现在最新的 Mamba 模型，这些全都是注意力机制的改良。

Speaker 1 00:54:21.450 
虽然看着眼花缭乱，但本质上解决的问题却非常简单，我一说你就懂了。其实注意力层最大的问题就是每个 Token 向量都需要和其他向量来一次计算。说 Token 烧向量，烧的就是这玩意。有一种办法是，仍然是每个 Token 之间都算一次，但是计算量相对减少一点，比如说想办法把向量缩短啊等等。这就是MQRA、GQRA、 MLA 所努力的方向，数量没变，但是难度降低了。

Speaker 1 00:54:48.880 
另一种办法呢，是计算的数量减少，比如说只计算一个固定窗口内的Token，或者呢，通过某种办法挑选一部分 Token 进行计算。这就是DSA、SWA、CSA、 HCA 思想的核心，也叫做稀疏注意力类型。还有另一种更极端的办法，就是直接把注意力机制的复杂度变成线性增长。比如说KDA、DeltaNet、Lightning、 Mamba 都属于这种。由于它实在太另类了，所以说人们把传统注意力和这种线性注意力混合使用的方式，又叫做混合注意力 Hybrid Attention。这块暂时就没有那么统一了，现在是百花齐放，谁也不知道哪个更好。也是由于实在太卷了，能创新的点早就被挖干净了。 Attention 死。

Speaker 1 00:55:33.510 
总结一下，位置编码归一化，残差连接， F F N，注意力，整个架构已经没有多少可以动刀子的地方了。当然呢，这期视频有点标题党，严谨点说是我觉得现在的语言模型领域，单纯依靠继续优化 Transformer 架构而带来模型能力上限的突破，这条路的空间已经不大了。从一年一度的斯坦福报告中也可以看出，开源模型和闭源模型的差距在不断缩小，各厂商之间的差距也在不断缩小。这其实很好理解，假如大模型的发展是线性的，它们的差距应该是保持不变的。假如是指数型的，它们的差距反而应该是越拉越大。只有发展是逐渐放缓的，差距才会逐渐缩小。同时呢，这也和我们的体感是差不多的，能大幅提升性能的简单手段。真的是已经越来越少了。

Speaker 1 00:56:22.340 
从更宏观的视角，我找到了 AI 发展的三个定律。 Skill on law 就是我们说的大力出奇迹，但是现在互联网的数据基本上已经被咱们人类挖干净了，算力和模型规模呢，也很难在短时间内提升，所以说遇到了瓶颈。 Bitern lesson 告诉我们，从长期来看，我们人类人为在模型架构层面设计的一些小巧思呀，在更大的模型面前反而是阻碍。更狠一点说就是人类的自以为是阻碍了模型的学习速度，尤其呢，是在现在大模型架构已经被人们弄了这么多刀了，即便是有新的想法，在这个定律面前也可能是一种负优化。

Speaker 1 00:56:58.220 
莫拉维克悖论就是说对。对人类很简单的，对机器很难。反过来，对机器很简单的，对人类反而很难。比如说机器在算术方面就秒杀人类，但是你让它缝个衣服试试，这就是世纪难题了。即便是在语言模型中，这个问题依然存在，整个底层的 transformer 架构其实并不一定是正确的。当然了，基于这些考虑，人们也早就在尝试其他的做法了，比如说多模态和世界模型。只不过现在有一些多模态，它只是做了一个模态对齐，最终呢，还是在利用大语言模型的便利。世界模型那就更模糊了，边界和定义都不清晰。

Speaker 1 00:57:32.440 
这个呢，我可以找时间单独出一期视频好好聊聊，这里混乱的很。不知道你看到这里有什么感受呢？这里的话，我也在视频最后稍稍叠个甲哈，虽然可能已经没有用了，视频中我之前有各种什么这个，那个，其实并不是对技术的不尊重哈，我恰恰是想用这种方式呢。给大家形象的展示一下现在大模型的一个架构变迁。

Speaker 1 00:57:57.300 
其实我们每天接触的新名词可能有很多，加上每一次一个新的技术出现的时候，总会带来一些过度的宣传吧。但是本期视频的话，你就会发现归根结底的话，其实也就是一些固定的套路，反过来调过去的去改，很少有完全出乎意料的一些花样。我觉得我们不应该高估单个技术的贡献，同时呢，也不应该低估技术的量变产生质变的一个过程。所以说如果觉得这期视频有帮到你的话，求个小小的三连哦。等后面再出现什么新技术的时候，你可以回看一下这期视频，从上帝视角重新审视一下，或许你能很快判断出这些新技术的地位和影响吧。好，感谢大家。

## 关键词

向量、架构、上下文、矩阵、归一化、点积、编码、线性、词表、神经网络、语言模型、模型架构、线性投影、训练数据、激活函数、编码方式、模型参数、强化学习
